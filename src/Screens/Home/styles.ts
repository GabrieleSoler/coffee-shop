import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    foodContainer: {
        flex: 1,
        flexDirection: "row",
        gap: 20,

    },
    container: {
        flex: 1,
        gap: 30,
        paddingTop: 20
    }
})