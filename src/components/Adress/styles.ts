import { StyleSheet } from "react-native";
import { THEME } from "../../styles/theme";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center"
    },
    text: {
      color: THEME.COLORS.BASE_LIGHT_GRAY
    },
    adress: {
        fontSize: 16
    }
});